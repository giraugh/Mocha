var str = `
		//IO Modules
			<-say x
			@console.log >> x:
			
			<-shout x
			@alert >> x:
			
			<-question x y
			@prompt >> x y:

			<-warn x
			@console.warn >> x:
			
			<-scream x
			@console.error >> x:
`;
eval(Mocha(str));