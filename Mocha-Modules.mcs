var str = `
		//IO Modules
			<-say x
			@console.log >> x end

			<-shout x
			@alert >> x end

			<-question x y
			@prompt >> x y end

			<-warn x
			@console.warn >> x end

			<-scream x
			@console.error >> x end
`;
eval(Mocha(str));
